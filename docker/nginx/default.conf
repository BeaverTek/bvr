server {
    listen [::]:80;
    listen 80;

    server_name .hsal.es; # root and subdomains

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    rewrite ^ https://$host$request_uri? permanent;
}
