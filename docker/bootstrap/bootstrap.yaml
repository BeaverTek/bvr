version: "3.3"

services:
  bootstrap:
    build: .
    volumes:
      - ../certbot/www:/var/www/certbot
      - ../certbot/files:/etc/letsencrypt
      - ./bootstrap.sh:/etc/bootstrap.sh
    ports:
      - 80:80
    environment:
      - email=hugo@fc.up.pt
      - root_domain=hsal.es
      - subdomains=social heard noise tube events boards forum wiki paste u survey matrix xmpp
    entrypoint: /bin/sh -c '/etc/bootstrap.sh'
